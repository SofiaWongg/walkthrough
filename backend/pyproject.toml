[project]
name = "backend"
version = "0.1.0"
description = "Walkthrough App Backend"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "firebase-admin>=6.0.0",
    "pydantic-settings>=2.0",
    "openai>=1.0.0",
]
